export enum LogoDisplay {
  flex = 'flex',
  none = 'none',
  block = 'block',
}

export enum ROUTES {
  MAIN = '/',
  LOGIN = '/login',
  REGISTRATION = '/registration',
  NOT_FOUND = '/404',
  BAD = '*',
  PROFILE = '/profile',
  ADMIN = '/admin',
}
