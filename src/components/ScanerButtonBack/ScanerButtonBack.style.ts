import { makeStyles } from '@mui/styles';

const useScanerButtonBackStyle = makeStyles({
  ScanerButtonBack: {
    top: '92%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
  },
});

export default useScanerButtonBackStyle;
