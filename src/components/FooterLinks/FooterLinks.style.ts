import { makeStyles } from '@mui/styles';

const useFooterLinksStyles = makeStyles({
  LinksList: {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
  },
  ListItem: {
    display: 'flex',
  },
});

export default useFooterLinksStyles;
