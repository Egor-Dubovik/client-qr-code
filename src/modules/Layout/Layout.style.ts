import { makeStyles } from '@mui/styles';

const useLayoutStyles = makeStyles({
  main: {
    position: 'relative',
    padding: '60px 0 0 0',
    flex: '1 1 auto',
  },
});

export default useLayoutStyles;
